# Ranking step (rank.py) — the ONLY runtime dependency.
# Deliberately minimal so `pip install -r requirements.txt` can never fail to
# reproduce the submission, on any Python 3.10+. rank.py imports only numpy + stdlib.
numpy>=1.26

# To REBUILD the precomputed artifacts (offline, untimed) you also need the
# heavier stack — see requirements-precompute.txt. Not needed to reproduce the CSV.
