# Live ranking step (rank.py) needs ONLY numpy + the Python stdlib.
# sentence-transformers / torch / transformers are required ONLY for the
# offline precompute step (precompute/build_embeddings.py, build_jd_vector.py).
#
# torch here is the CPU build (no CUDA). On Windows/Linux `pip install torch==2.1.2`
# resolves to the CPU wheel by default; if you need to force it:
#   pip install torch==2.1.2 --index-url https://download.pytorch.org/whl/cpu

numpy==1.26.2

# --- precompute-only (not used during the timed ranking step) ---
sentence-transformers==3.4.1
torch==2.1.2
transformers==4.44.2
huggingface-hub==0.36.2
scikit-learn==1.3.2
scipy==1.13.0

# optional: only used by precompute/build_reasoning.py IF ANTHROPIC_API_KEY is set
# anthropic>=0.40
